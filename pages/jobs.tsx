import React from 'react';
import { Layout, Text } from '../components';

const Jobs = ({ posts }) => (
  <Layout>
    <Text fontSize="6xl" color="inherit">
      Developer Jobs
    </Text>
  </Layout>
);

export default Jobs;
